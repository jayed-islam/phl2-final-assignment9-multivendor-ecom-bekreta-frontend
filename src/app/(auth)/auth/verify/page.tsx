import ResetPassword from "@/sections/auth/reset-password-view";
import React from "react";

export const metadata = {
  title: "Verify",
};

const Page = () => {
  return <ResetPassword />;
};

export default Page;
